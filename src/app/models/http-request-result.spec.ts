import { HttpRequestResult } from './http-request-result';

describe('HttpRequestResult', () => {
  it('should create an instance', () => {
    expect(new HttpRequestResult()).toBeTruthy();
  });
});
